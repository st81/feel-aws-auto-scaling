version: 2.1
orbs:
  node: circleci/node@1.1
jobs:
  build:
    executor:
      name: node/default
      tag: '15.0.1'
    steps:
      - checkout
      - run: 
          working_directory: backend
          command: |
            npm install
      - run: 
          working_directory: backend
          command: |
            npm run test
